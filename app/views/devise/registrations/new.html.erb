
<!-- The row to wrap them all -->
<div class="row">
  <!-- Form starts here -->
  <form>

    <%= form_for(resource, as: resource_name, url: registration_path(resource_name)) do |f| %>
    <%= devise_error_messages! %>

    <!-- Left column -->
  <div class="large-4 columns">
       
         <%= f.label :avatar %>
         <%= f.file_field :avatar %>
  </div>
  <!-- End of left column -->


  <!-- Keeping it neat with 8 columns -->
  <div class="large-8 columns">



    <h2>Sign up</h2>

    <!-- Enter name -->
    <div class="row">
    <div class="large-12 columns">

        <%= f.label :name %><br />
        <%= f.email_field :name, autofocus: true, class: 'form-control', placeholder: "Enter name" %>
    </div>
    </div>

    <!-- Enter email -->
    <div class="row">
      <div class="large-12 columns">
        <%= f.label :email %><br />
        <%= f.email_field :email, autofocus: true %>
      </div>
    </div>

    <!-- Enter password -->
      <div class="row">
        <div class="large-6 columns">
          <%= f.label :password %> 
          <% if @validatable %>
          <em>(<%= @minimum_password_length %> characters minimum)</em>
          <% end %><br />
          <%= f.password_field :password, autocomplete: "off" %>
      </div>

    <!-- Enter password again for confirmation -->

      <div class="large-6 columns">
        <%= f.label :password_confirmation %><br />
        <%= f.password_field :password_confirmation, autocomplete: "off" %>
      </div>
      </div>

      <div class="row">
        <div class="large-12 columns">

        <%= f.submit "Sign up", class: "button success" %>

    <% end %>
      </div>
      </div>
      
    <div class="row">
      <div class="large-12 columns">
       <%= render "devise/shared/links" %>
      </div>
    </div>


  </form>

  <!-- Form ends here -->
  
  <!-- Keeping it neat with 8 columns -->
  </div>
<!-- The row to wrap them all -->  
</div>
