  
  <%= link_to user, class: 'media-left' do %>
    <%= image_tag(user.avatar.small.url, class: "media-object") if user.avatar? %>
  <% end %>


      <%= link_to user.name, user %>
    </h4>

    <small>
      <%= pluralize(user.bookmarks_count, 'bookmark') %>
    </small>
  </div>

</div>